name: 'gravlytics_transforms'
version: '1.0.0'
config-version: 2

profile: 'gravlytics_clickhouse'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  gravlytics_transforms:
    staging:
      +materialized: view
    marts:
      +materialized: table
      +engine: 'ReplacingMergeTree'
